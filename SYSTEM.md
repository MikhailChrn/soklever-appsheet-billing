# SYSTEM.md — СО Клевер Биллинг

**Версия:** 0.4.9 (11.05.2026)
**Назначение документа:** долгоживущая память о бизнес-модели и назначении системы. Описывает «зачем» и «что», не «как».

**Что НЕ в этом документе:** конкретные формулы VC, имена slices/actions, состав колонок таблиц — всё это в `ARCHITECTURE.md`. Метафоры и термины — в `GLOSSARY.md`. Регламенты работы — в `REGULATIONS.md`.

**Когда обновлять:** при изменении бизнес-модели — новые роли, новые сущности, изменение бизнес-процесса. НЕ при технических правках.

> Помечено `[?? уточнить]` — пробелы для заполнения владельцем.

---

## §1. Назначение системы

### §1.1. Что это

Система биллинга для частной школы **СО Клевер**. Учёт начислений за обучение, платежей от семей, долгов и расходов школы.

**Этап развития:** Этап 0 — «Google-прародитель». Временное решение на стеке Google Sheets + AppSheet. В перспективе — переход на основной продукт (Java + React, отдельный большой проект).

### §1.2. Кто пользователи

- **Владелец и основной разработчик системы:** Михаил.
- **Главный пользователь:** супруга Михаила — основатель и операционный директор школы. Работает с дашбордом, принимает платежи, контролирует должников, отвечает за финансовую картину.
- **Администраторы школы (роль OPERATOR):** вносят текущие расходы (свои закупки, оплаты педагогам и т.п.). Доступ ограничен одним view.

### §1.3. Дедлайн запуска

**01.06.2026** — система должна работать с реальными данными. Сначала летний цикл (Р-11), потом обычная эксплуатация.

### §1.4. Что система решает

1. Учёт привязок ученик-тариф.
2. Автоматическое начисление платы в начале каждого месяца через закрытие периода.
3. Учёт платежей от семей (модель сосудов, без привязки к конкретному начислению).
4. Подсчёт долгов на лету (текущая задолженность + дебиторская задолженность по Р-6).
5. Учёт расходов школы по категориям и периодам.
6. Подведение финансового итога по закрытым периодам и за учебный год.
7. Дашборд с ключевыми цифрами для руководителя (Р-9).

### §1.5. Что система НЕ решает

- Не учитывает посещаемость, успеваемость, расписание.
- Не общается напрямую с родителями (нет личного кабинета).
- Не делает прогнозы и аналитику «потенциальных» сценариев.
- Не хранит историю изменений тарифов, состава классов и т.п.

---

## §2. Принципы системы

### §2.1. Модель сосудов

Платежи и начисления работают как два сообщающихся сосуда. Долг = разность уровней. Платёж не привязан к начислению.

→ детали см. `GLOSSARY.md` §1.1.

### §2.2. Твёрдая земля и живая зона

Закрытые периоды — твёрдая земля. Текущий открытый период — живая зона. Должником считается тот, у кого есть долг по твёрдой земле.

→ детали см. `GLOSSARY.md` §1.2.

### §2.3. Суровая правда жизни без шума

Простота важнее «гибкости на потом». Не закладываем механики, которые не приносят пользы прямо сейчас.

→ детали см. `GLOSSARY.md` §1.3.

### §2.4. Параллельные системы представления

- **Sheets** — источник истины для владельца (Михаил).
- **AppSheet** — операторский UI для основного пользователя (супруга) и администраторов (OPERATOR).

Разные инструменты для разных людей. Лист `dashboard` в Sheets к AppSheet **не подключаем**.

---

## §3. Акторы и роли

### §3.1. Технические роли (в системе)

| Роль | Кто | Доступ |
|---|---|---|
| **ADMIN** | Михаил, супруга | Всё |
| **OPERATOR** | Администраторы школы | Только view «расходы» + свои внесённые расходы |

Роль определяется через таблицу `users` (`email`, `role`). На каждый view, скрытый от OPERATOR — `Show_If` с проверкой ADMIN. На slice `expenses_filtered` — ролевой фильтр в Row filter.

→ конкретная реализация — `ARCHITECTURE.md`.

### §3.2. Процессные роли (в работе над системой)

| Роль | Кто | Что делает |
|---|---|---|
| **Владелец** | Михаил | Принимает все решения, носитель доменного знания |
| **SubGeneral** | Opus | Стратег: архитектура, методология, формулы, брифинги |
| **Sonnet** | Sonnet | Исполнитель: руками в AppSheet/Sheets по брифингу |

→ регламент работы — `REGULATIONS.md`.

---

## §4. Сущности (бизнес-модель)

Технические детали (колонки, типы, формулы) — в `ARCHITECTURE.md`. Здесь — только смысл.

### §4.1. Семья (family)

Логическая единица оплаты. Имеет название, телефон, email. Долги детей семьи **агрегируются** в семейный долг — он и считается.

### §4.2. Ученик (student)

Конкретный ребёнок в школе. Принадлежит семье, имеет класс, дату начала обучения, статус, дату рождения (опционально, добавлено в pre-#08.1).

**Статус:**
- `"Активен"` — учится сейчас, при закрытии периода создаются начисления.
- `"Выбыл"` — закончил обучение, новых начислений нет, накопленный долг — за семьёй.

**При выбытии** (см. §5.4) очищается `class_group` и удаляются все привязки `student_tariffs` (история привязок не сохраняется — Р-14).

**Когда выбывший полностью рассчитался** — скрывается из основного списка учеников через Slice `students_visible` (фильтр `OR(status="Активен", debt<>0)`). Карточка остаётся в БД для истории.

### §4.3. Тариф (tariff)

Регулярная стоимость обучения в месяц. **Все тарифы по определению регулярные** — иной механики нет. На момент 07.05.2026 — 9 базовых тарифов:
- Базовые по классам: «2 класс, база», «3 класс, база», «5 класс, база», «7 класс, база».
- Продлёнки: «Мини», «Стандарт», «Оптимал», «Полный».
- Летний взнос (T009 — `[?? уточнить судьбу — оставить или удалить, см. v10 §7.4]`).
- T010 «Летний тариф» — будет создан в брифинге #08.

**Разовые сборы** (например, экскурсия, разовый материал) реализуются через **точечное начисление с `charge_type = "Ручное"`** в карточке ученика — не через отдельный тариф. Колонка `tariff_type` (с Enum-значениями «Регулярный»/«Единоразовый») была рудиментом — значение «Единоразовый» в проекте ни разу не использовалось. Удалена 11.05.2026 в pre-#08.4 (см. §8.1 ТД-5).

### §4.4. Привязка ученик-тариф (student_tariff)

«Ученик X учится по тарифу Y». Один ученик может иметь несколько привязок (база + продлёнка). **Без дат, без истории** (Р-14). Удаление строки = со следующего закрытия начисления по этому тарифу не создаются.

### §4.5. Начисление (charge)

«Ученик должен Z рублей за месяц YYYYMM». Создаётся при закрытии периода (`charge_type = "Авто"`) или вручную через карточку ученика (`charge_type = "Ручное"`). Сумма фиксируется на момент создания.

**`charge_type` после #07.3:** только два значения — «Авто» и «Ручное». Значение «Корректировка» удалено в #07.3 — корректировки реализуются как «Ручное» с отрицательной суммой.

### §4.6. Платёж (payment)

Внесение оплаты от семьи. Привязан к ученику, без поля `period` (Р-1). Работает модель сосудов.

### §4.7. Расход (expense)

Финансовая операция школы. Не связан с учениками. Имеет **период отнесения** (`period`, для дашборда) и **дату факта** (`expense_date`). Категории: Аренда, Педагоги, Коммуналка, Уборка, Хозтовары, Канцтовары, Мероприятия, Оборудование, Прочее.

### §4.8. Открывающие балансы (opening_balances)

Историческая таблица для миграции с другой системы. Позволяет указать «ученик X на момент входа в систему уже должен/переплатил Y рублей».

`[?? уточнить — на момент 07.05.2026 эта таблица планируется к использованию в проде, или останется пустой?]`

---

## §5. Бизнес-процессы

### §5.1. Закрытие периода

**Когда:** в последних числах закрываемого месяца (Р-7). Например, апрель закрываем 28-30 апреля, не 5-10 мая.

**Кто:** ADMIN.

**Шаги:**
1. ADMIN открывает view «скрипты» (MENU NAVIGATION), нажимает «Закрыть период».
2. Confirmation: «закрыть период X, открытый — Y».
3. Bot `bot_close_period` проверяет (Branch с календарной привязкой): можно ли закрывать. Если уже закрыто — сообщает «уже закрыто», обновляет `last_close_message`.
4. Если можно — ForEach по `student_tariffs` (фильтр: активные ученики + регулярные тарифы), создаёт `charges` за следующий период.
5. Записывает в `settings[last_close_at]` и `settings[last_close_message]` результат.
6. `current_period` сдвигается автоматически (вычисляется по `charges`).
7. Дашборд обновляется автоматически. Плитка «Текущее закрытие» показывает результат.

**Платформа:** AppSheet Bot, не Apps Script. Триггер — служебная колонка `settings[last_close_trigger]` через Data Change Event. Технические детали — `ARCHITECTURE.md` §4.

### §5.2. Регистрация платежа

Через карточку ученика → «+ платёж» → форма (дата, сумма, тип, комментарий).

`payment_date` редактируется (можно проставить задним числом для стыков месяцев), default = TODAY() (Р-4).

### §5.3. Регистрация расхода

Через view «расходы» → `+`. Имеет период отнесения (для дашборда), дату факта, категорию, сумму, получателя, комментарий.

OPERATOR может вносить расходы — видит только свои.

### §5.4. Выбытие ученика

Через карточку ученика → кнопка «выбытие» → confirmation с именем и текущим периодом → форма выбора даты → транзакция:
- `end_date` = выбранная дата,
- `status` = `"Выбыл"`,
- `class_group` = пусто,
- удаление всех `student_tariffs` ученика.

При следующем закрытии периода — этому ученику начислений уже не будет (фильтр `[status]="Активен"` в боте). Текущее начисление за уже открытый период **не пересчитывается** (начисления не дробим).

→ реализация — pre-#08 (06-08.05.2026, базовый action) + pre-#08.1 (09.05.2026, расширение). Технические детали — `ARCHITECTURE.md` §3.6.

### §5.5. Просмотр должников

Через view «Должники» (Primary меню). Семьи с `family_overdue_debt > 0`. По каждой — телефон/email для связи, кнопки звонка/email/SMS.

### §5.6. Летний цикл (Р-11)

**Ключ к пониманию дат:** бот `bot_close_period` создаёт начисления **за следующий период**, а не за закрываемый. То есть закрытие мая → создаются **июньские** charges. Поэтому для получения летнего тарифа в июньских начислениях массовая замена привязок должна отработать **ДО** закрытия мая.

| Дата | Действие |
|---|---|
| ≤27 мая | **Массовая замена `student_tariffs` на T010** (через скрипт, брифинг #08): для каждого активного ученика — удалить все текущие привязки, создать одну новую на T010. |
| 28–31 мая | Обычное закрытие мая → создаются **июньские charges по T010**. |
| 30 июня | Закрытие июня → июль по T010. |
| 31 июля | Закрытие июля → август по T010. |
| 31 августа | Закрытие августа (август — последний летний месяц по T010). |
| 01 сентября | **Ручная** пересборка: классы, состав учеников, восстановление индивидуальных классных тарифов в `student_tariffs`. |
| 28–30 сентября | Закрытие сентября — стартует новый учебный год. |

**Итог по T010:** июнь, июль, август — три летних месяца с одинаковой суммой 5 000 ₽ (временное значение, операционный директор уточнит позже).

**Возврат к классным тарифам** — ручной 01 сентября при пересборке. Автоматического обратного скрипта не делаем (`SYSTEM.md §2.3` — «суровая правда без шума»).

**T009 «Летний взнос»** — удаляется в #08 как дубликат T010 (привязок нет, тариф никогда не использовался).

### §5.7. Закрытие учебного года (Р-10)

Никакой специальной механики. В августе:
1. Снимаем физический остаток денег.
2. Проводим как `expenses`.
3. Накопительные итоги обнуляются естественным образом.
4. 1 сентября — нулевая твёрдая земля.

### §5.8. Disaster Recovery

Восстановление при потере или повреждении приложения. Подробно — `RECOVERY.md` (создаётся в #09).

---

## §6. Дашборд (Р-9)

Дашборд для **руководителя школы** (супруги Михаила). Отвечает на 4 вопроса:

1. Сколько денег в казне сейчас?
2. Сколько ещё придёт в этом месяце?
3. Сколько нам должны?
4. Что с расходами в этом месяце?

**Структура — три блока сверху вниз:**

### §6.1. Блок 1. Живая зона (текущий открытый период)

- Поступления за открытый период.
- Расходы за открытый период.
- Всего начислено в открытом периоде.

### §6.2. Блок 2. Долги (твёрдая земля)

- Количество должников-семей.
- Общий долг.

По Р-6: долг считается **только** по периодам строго раньше `current_period`.

### §6.3. Блок 3. Накоплено за учебный год (твёрдая земля)

- Получено в закрытых периодах (с 1 сентября).
- Израсходовано в закрытых периодах.
- Свободные средства = разность.

**Прогнозирование не закладываем** (Р-9, §2.3).

### §6.4. Дополнительная плитка «Текущее закрытие»

Введена в брифинге #07.3, текст уточнён мини-патчем #11. Показывает:
- `last_close_at` — timestamp последнего закрытия.
- `last_close_message` — текст результата.

**Текст после фикса #11:** «Период {dash_пред_период} закрыт. Открытый период: {current_period}. Создано начислений: N.» — корректно различает «закрытый» (предыдущий) и «открытый» (текущий после сдвига) периоды.

Технические детали (Slice, View, формулы) — `ARCHITECTURE.md` §4.12.

---

## §7. Стек и платформа

### §7.1. Хранилище данных

**Google Sheets:** `2026-04-17 soklever-google-ancestor-v6 (main)`. 13 листов:
- 8 рабочих: families, students, tariffs, student_tariffs, charges, payments, expenses, opening_balances.
- 3 служебные: settings, expense_categories, users.
- 2 справочника: ref_periods (60 строк), ref_classes (12 строк) — введены в #6 при закрытии ТД-1.

`debts` и `dashboard` листы — рудимент эпохи до AppSheet, к удалению.

**Канал аварийной правки данных задним числом.** AppSheet UI запрещает любое изменение charges из закрытых периодов (см. §8.4, pre-#08.5 §4.4). Это правило держит «твёрдую землю» (Р-7) для повседневной работы ADMIN'а и OPERATOR'а. Аварийный канал для исключительных случаев — **прямое редактирование Google Sheets** владельцем (Михаил, как администратор источника, а не как ADMIN в терминологии приложения). Это сознательный двухуровневый дизайн: пользователь приложения не может сломать историю случайной правкой, владелец данных — может, под свою ответственность.

### §7.2. Интерфейс и логика

**AppSheet:** приложение `soklever-root-billing v.1.0`. Основной UI для повседневной работы. Версия на 11.05.2026 — 1.000535 (после pre-#08.6; платформа AppSheet обновилась в ходе сессии с 1.000523).

**Закрытие периода** — через AppSheet Bot (внутри AppSheet, без Apps Script). Триггер — служебная колонка `settings[last_close_trigger]`. Технические детали — `ARCHITECTURE.md` §4.

### §7.3. Окружение

Только облачное (Google + AppSheet). Локальных компонентов нет.

---

## §8. Открытые архитектурные вопросы (на 09.05.2026)

### §8.1. Технический долг

**ТД-1. Enum-периоды и Enum-классы** ✅ **закрыт в #6** (03.05.2026).
Реализовано через справочники `ref_periods` и `ref_classes` (Р-16). 9 колонок переведены в Ref. Деталь — `ARCHITECTURE.md` §5.1.

**ТД-3. Косметика карточки тарифа** — `tariff_id` (T001) в шапке. Не блокер. Если появится — Show? = false.

**ТД-5. Чистка рудиментов.** Сводный микро-брифинг после #8. К удалению:
- Таблица `debts` в AppSheet (мёртвая по Р-5).
- Лист `debts` в Sheets.
- Лист `dashboard` в Sheets.
- `opening_balances` — если в проде всегда пустая (порядок важен: сначала вычистить из формул долга, потом таблицу).
- ✅ Колонка `tariffs[tariff_type]` — удалена 11.05.2026 в pre-#08.4. Правки: фильтр в `bot_close_period` (формула `SELECT(student_tariffs[assignment_id], [student_id].[status] = "Активен")`), дополнительно VC `settings[current_period]` (всплыло при Regenerate Schema). См. §4.3.
- View-фильтры `charges_filtered`, `payments_filtered`, `expenses_filtered` (slice + view) — теоретически заменяются встроенным фильтром AppSheet. На практике (10.05.2026) — первая попытка дала непредсказуемый список фильтруемых колонок на view «начисления». Перенесено в исследование §8.4, к удалению **не назначено** до прояснения правила.

### §8.2. Концептуальные вопросы

- **О-3.** Эмпирические наблюдения после старта пилота. Открыт. Будет копиться сам после пилотного внедрения.
- **О-4.** Скрипт массовой смены тарифа на лето — **в работе** (брифинг #8, дедлайн 01.06.2026).
- **О-6.** Тип `charges[period]`: Text vs Number — ✅ **закрыт в #6** (Ref на `ref_periods`, ключ Text — BP-36).

### §8.3. Открытые `[?? уточнить]`

- `opening_balances` — планируется к использованию в проде или останется пустой? (§4.8)
- ✅ **закрыто 09.05.2026:** T009 «Летний взнос» — удаляется в #08 как дубликат T010.
- ✅ **закрыто 09.05.2026:** июнь-июль-август — все три месяца по T010 (см. §5.6).
- ✅ **закрыто 09.05.2026:** T010 = 5 000 ₽ (временно, операционный директор уточнит позже).

### §8.4. Технические открытые вопросы

- **VC внутри Bot transaction** — pre-commit состояние. В NO-сообщении бота `dash_долги_количество` давало `1`, на дашборде показывало `3`. Решено в #11: убрать «Должников» из NO-сообщения.
- **View-фильтры** (`фильтр начислений`, `фильтр платежей`, `фильтр расходов`) — компромисс времён, когда не знали про встроенный фильтр AppSheet. В pre-#08.1 очищены от лишних кнопок (Edit/Delete/Add убраны), Slices переведены в Read-Only. **Решение от 10.05.2026 (после первой попытки исполнения pre-#08.4):** оставлены работать. Замена на встроенный фильтр отложена — на view «начисления» встроенный фильтр предложил не те колонки, что на «ученики» (где ресёрч проходил). Правило формирования списка фильтруемых колонок AppSheet не понято, гипотезы «Manual vs Automatic Column order» и «Searchable на колонке» не подтвердились. Открытое исследование, не задача. До прояснения — не трогать.

  **Рабочая гипотеза** (10.05.2026, не верифицирована): AppSheet **исключает Ref-колонки** из встроенного фильтра, если Ref-таблица — «живая сущность» (большая, динамическая, как `students` или `ref_periods` с 60 строками), и **включает**, если Ref-таблица — компактный справочник (как `ref_classes` с 12 строками — этот в «учениках» отфильтровался). Возможно, фильтр работает только по скалярам строки (Text, Number, DateTime, Enum) и Ref к справочникам с малым кардинальным числом — это похоже на защиту от N+1 при фильтрации. Проверка: создать тестовый view на charges, попробовать включить фильтрацию по `period` явно через настройки view; альтернативно — сравнить поведение Ref `class_group` (ref_classes, 12 строк) и Ref `period` (ref_periods, 60 строк) на одинаковых view. Не сейчас.

  **Дополнительное наблюдение от Sonnet'а** (11.05.2026, в ходе внебрифинговых правок pre-#08.4): на временном table view `начисления` (charges, menu) встроенная Filter panel предложила **только** `описание` и `создан` — не предложила ни `period` (Ref на ref_periods), ни `student_id` (Ref на students), ни `amount` (Number), ни `charge_type` (Enum). Это **противоречит** гипотезе «фильтрует Text/Number/DateTime/Enum» — Number и Enum здесь скаляры, но не появились. View удалён после получения обновлённого брифа. Правило по-прежнему не установлено.
- ✅ **Колонка `tariffs[tariff_type]`** — закрыт 11.05.2026. pre-#08.3 (10.05) скрыл колонку из форм (Initial value «Регулярный»), pre-#08.4 (11.05) удалил полностью: правка фильтра в `bot_close_period`, удаление колонки в Sheets, Regenerate Schema. Дополнительная ссылка в `settings[current_period]` (VC) всплыла как Errors & Warnings при Regenerate Schema и была исправлена Sonnet'ом в той же сессии — фильтр `[tariff_id].[tariff_type] = "Регулярный"` снят, формула стала `INDEX(SORT(SELECT(charges[period], TRUE), TRUE), 1)`. Глобальный поиск `tariff_type` — 0 результатов. См. ТД-5.
- **Раздельные заголовки Add/Edit form view** — невозможно в текущей версии AppSheet (май 2026). Подробно — `sonnet-skills/appsheet-setup-SKILL.md` Q.17. Принято как ограничение платформы, компромисс — VC `Section_Header` с универсальным заголовком «Создать/редактировать …» (Q.16). Внедрено в pre-#08.3 §4.1 (10.05.2026) после провала трёх путей: LINKTOROW + override Edit, переименование с `z_`-префиксом, Action `App: open a form to add a new row` (без выбора Target в текущем редакторе).
- ✅ **Запрет Edit для charges из закрытых периодов** — закрыт 11.05.2026 в pre-#08.5 §4.4. Изначально формулировался как «запрет Edit для `charge_type = "Авто"`», в диалоге 11.05 переформулирован: твёрдая земля = весь закрытый период, независимо от `charge_type`. Реализация: на системном Edit `charges` свойство **Only if this condition is true** доступно (не read-only) — поставлено `[period] = INDEX(settings[current_period], 1)`. Test condition пройден. Ручных charges в закрытом периоде по нормальному флоу не возникает; корректировки задним числом — новой Ручной charge в открытом периоде (Р-14), либо аварийно через Google Sheets администратором (см. §7.1). Связанный хвост — защита от **создания** charges в закрытом периоде через форму — pre-#08.6.

**Замеченные хвосты от Sonnet'а в pre-#08.3 (10.05.2026, для последующих мини-патчей):**
- ✅ **`billing_frequency` в формах тарифа** — закрыт 11.05.2026 в pre-#08.6 §4.1. Столбец удалён из листа `tariffs` (Google Sheets, столбец D), Regenerate Schema без ошибок и предупреждений. Глобальный поиск — 0 результатов.
- ✅ **`tariff_type` в `tariffs_Detail`** — закрыт 10.05.2026, владелец убрал вручную в ходе сессии pre-#08.3 (зафиксировано в отчёте §7).
- ✅ **Чистка `students_visible_Form`** — закрыт 11.05.2026 в pre-#08.5 §4.2. Подтверждено: primary view «ученики» построена на slice `students_visible`, живая форма — `students_visible_Form` (`students_Form` мёртвая, не используется). Из Column order убраны: `debt`, `overdue_debt`, все Related-блоки. Осталось: `_form_header_students`, `family_id`, `first_name`, `last_name`, `birth_date`, `class_group`, `status`, `start_date`, `end_date`, `comment`.
- ✅ **`students[birth_date]` Initial value** — закрыт 10.05.2026, владелец очистил вручную в ходе сессии pre-#08.3 (зафиксировано в отчёте §7).
- ✅ **Header column для карточки платежа из карточки ученика** — закрыт 11.05.2026 в pre-#08.5 §4.3 по факту. Нашли два view (`платежи_ученика_inline` на slice `recent_payments` и системный `payments_Inline`) — оба типа **table**, Primary/Secondary/Summary header у table view недоступны. Однако в карточке ученика блок «текущие платежи» уже отображает таблицу с колонками «тип платежа / сумма / дата платежа / комментарий» — цель (информативный inline) фактически достигнута через колоночный режим table. Подтверждено владельцем 11.05: «Сейчас в карточке ученика корректно отображается».
- **Detail view charges/payments через `*_filtered`** — обнаружено 10.05.2026 в pre-#08.3 §4.2: пользователь открывает карточки начислений и платежей через view-фильтры `charges_filtered`/`payments_filtered`, а не через системный `*_Detail`. Header column был установлен в `charges_Detail`/`payments_Detail`, но визуально проявится только при прямом открытии. Нужно проверить путь открытия в проде и при необходимости настроить Header column на фактически используемом Detail view. Связано с pre-#08.4 (там планируется удаление `*_filtered` после ресёрча встроенного фильтра — может закрыть этот хвост естественным образом).
- **Расхождение charges 5 vs 6** в сценарии 3 теста #07.3 — не блокер, не расследовано.
- **Error-run `bot_close_period`** 04.05 в 15:45:47 — не расследован, не блокер.
- **`_ComputedName` дублировался в списке полей карточки ученика** — ✅ **закрыт 09.05.2026.** Решение: на колонке `_ComputedName` снять Show, оставив LABEL. Шапка Detail view продолжает рендерить `_ComputedName` как Header column независимо от Show. Правки делать в `students_visible_Detail` (а не `students_Detail` — primary view «ученики» построена на slice `students_visible`, system-generated Detail слайса — самостоятельный). См. `sonnet-skills/appsheet-setup-SKILL.md` §Q.14, §R.
- **VC игнорируют Column order в Detail view** — частично уточнено 09.05.2026. AppSheet рендерит **VC-скаляры** (`debt`, `overdue_debt`) после физических колонок, но **до Related-блоков**. То есть фактический порядок: физические → VC-скаляры → Related (платежи, начисления). Полностью пересобрать порядок (например, поднять `debt` на самый верх) нельзя — обход не найден. На практике для карточки ученика стандартный порядок оказался приемлемым.
- **Предупреждение в `students_withdrawal_form`** — `Table students_withdrawal does not allow new entries`. Информационное, работе не мешает. Если переключить Slice на Updates+Adds — появится кнопка «+», что хуже. Оставлено как есть.

**Хвосты от Sonnet'а в pre-#08.5 (11.05.2026):**
- ✅ **Защита от создания charges в закрытом периоде** — закрыт 11.05.2026 в pre-#08.6 §4.2. На `charges[period]` поставлены Initial value `INDEX(settings[current_period], 1)` и Valid_if со списком разрешённых периодов: `SELECT(ref_periods[period], AND(NUMBER([period]) >= NUMBER(INDEX(settings[current_period], 1)), NUMBER([period]) <= NUMBER(INDEX(settings[current_period], 1)) + 3))`. Окно «current + 3» (ретро запрещён §4.4). Дополнительно `ref_periods` переведена в Read-Only — исчезла кнопка «+ New» в Ref-dropdown.
- ✅ **Удаление `billing_frequency` из Sheets** — закрыт в pre-#08.6 §4.1 (см. выше).

**Хвосты от Sonnet'а в pre-#08.6 (11.05.2026):**
- ⚠️ **Delete не виден на мобильном при заходе через filter-view** для `charges` и `expenses` — открытый плавающий дефект (11.05.2026). Кэш-гипотеза опровергнута: полный logout/login на мобильном не помог. Уточнённый паттерн — зависит от **точки входа**: через filter-view (`фильтр начислений`, `фильтр расходов`) → Delete отсутствует; через карточку ученика (payment) или меню «расходы» (список) → Delete виден. На десктопе во всех маршрутах Delete на месте. Не блокер — десктоп остаётся основным admin-маршрутом, Show_If держит твёрдую землю независимо от рендера. Зафиксировано в SKILL R.3 как floating defect. К дальнейшему расследованию не назначено по решению владельца.
- **Ревизия всех slice'ов проекта на Read-Only** — открыт. В pre-#08.6 обнаружен паттерн: slice с Update mode = Read-Only **блокирует** отображение системных Edit/Delete в slice-Detail, даже при корректных Show_If на самих actions. Исправлено для трёх slice'ов (`charges_filtered`, `payments_filtered`, `expenses_filtered`). Стоит пробежать остальные slice'ы проекта на тот же предмет.
- **Ревизия всех Valid_if на колонках с Ref на `ref_periods`** — открыт. В `expenses[period]` Valid_if содержал `- 3` на нижней границе (разрешал ретро) — исправлено в pre-#08.6. Проверить, нет ли аналогичного в других колонках, где есть Ref на `ref_periods`.

### §8.5. Долгосрочное

- Обнуление баланса по учебным годам — после первого года эксплуатации.
- Пересмотр Р-4 (даты платежа задним числом) на основе реального опыта оператора.
- Миграция данных в основной продукт (после готовности Java + React стека).
- Регламент сборки бэкапа SubGeneral — обсуждалось в v9 §6.6 и v10. Гипотеза: дословные формулы и составы таблиц должны жить в `ARCHITECTURE.md`, а не пересказываться каждый раз в бэкапе. Закрыто запуском базы знаний 07.05.2026.

---

## §9. История эволюции системы

Краткая хронология ключевых решений. Полные ADR — в каждом бэкапе SubGeneral в `subgeneral-backups/`.

| Дата | Решение/Событие |
|---|---|
| Апрель 2026 (v3) | Базовая модель данных, AppSheet прототип, метафора сосудов |
| 27.04 (v4) | Дашборд первой версии, BP-1..BP-22 |
| 28.04 (v5) | ADR Р-1..Р-11, метафоры твёрдой земли и суровой правды, OPERATOR-роль |
| 29.04 (v6) | Реализация Р-1, Р-6, Р-12, Р-13. Цветовая индикация |
| 01.05 (v7) | Р-14 (student_tariffs без дат), Р-15 (регламент бэкапов), удалены `valid_from`/`valid_to` |
| Ранний май | Закрытие периода через AppSheet Bot (вместо Apps Script). Каскадирование (#6.1 v10) |
| 03.05 (v8) | Архсессия по ТД-1, рефакторинг Enum→справочники (#6), §Р-16, BP-36..BP-42 |
| 04.05 (v9) | Реализация закрытия периода через AppSheet Bot (#7), Branch с защитой |
| 05–06.05 (v10) | Каскадирование Bot — диагностика и фикс. Плитка «Текущее закрытие». #07.3 закрыт |
| 06.05 вечер | Мини-патч §7.1: новый текст NO-сообщения с +1 для следующего периода |
| 07.05 (v11) | pre-#08 (UX-правки), pre-#08.1 (расширенные UX-правки), запуск базы знаний на GitHub |
| 08.05 (v12.1) | Переход на Claude Code, реструктуризация README/CLAUDE, sonnet-skills/ |
| 08.05 (v12.2) | Реструктуризация корневых файлов: ARCHITECTURE.md и REGULATIONS.md созданы; SYSTEM.md → v0.3, GLOSSARY.md → v0.2 |
| 06–08.05 | Sonnet pre-#08: базовый action «Выбытие» (`end_date=TODAY()`, `status="Выбыл"`, Confirmation), форма «Новый ученик» через Column order, `families__add_student` через `LINKTOFORM`. Версия 1.000429 → 1.000441 |
| 09.05 (pre-#08.1) | Расширение «Выбытия» (ручная дата через `LINKTOROW` + Form Saved + Grouped action, очистка `class_group`, удаление `student_tariffs`); добавлены `students[birth_date]`, Slice `students_visible`; унификация Detail view (`*_id` скрыты, `created_at` → «создан», Delete на расходах); очистка view-фильтров. Версия 1.000444 → 1.000483 |
| 09.05 (v13) | SYSTEM.md → v0.4, ARCHITECTURE.md → v0.2, GLOSSARY.md → v0.3, sonnet-behavior-SKILL.md (v0.1) создан, appsheet-setup-SKILL.md обновлён (LINKTOROW, Grouped+cross-table) |
| 10.05 (pre-#08.3) | UX-патч заголовков форм. Принят компромисс — VC `Section_Header` с универсальным заголовком «Создать/редактировать …» (раздельные Add/Edit невозможны в AppSheet, см. SKILL Q.16/Q.17). Скрыт тумблер `tariff_type` из форм, Initial value «Регулярный». Detail view для charges/payments — Header column установлен. Внебрифинговые правки владельца: `tariff_type` убран из `tariffs_Detail`, `students[birth_date]` Initial value очищен. Версия 1.000489 → 1.000507. SYSTEM.md → v0.4.4, GLOSSARY.md → v0.3.2, REGULATIONS.md → v0.3 (инциденты 7-8, §7 «Дисциплина SubGeneral»), appsheet-setup-SKILL.md → v2.16 (Q.16, Q.17). |
| 11.05 (pre-#08.4) | Удалена колонка-рудимент `tariffs[tariff_type]`: правка фильтра в `bot_close_period` (`SELECT(student_tariffs[assignment_id], [student_id].[status] = "Активен")`), дополнительно правка VC `settings[current_period]` (всплыла при Regenerate Schema), удаление колонки в Sheets. Внебрифинговое от Sonnet'а: создание/удаление временного view `начисления` (по первой редакции брифа) и наблюдение по Filter panel — Number и Enum не появились в фильтруемых колонках, гипотеза по Ref-only ограничению не подтверждена. Финальный бэкап не сделан (тестовая схема, решение владельца). Версия 1.000507 → 1.000518 (фактическая на старте pre-#08.5). |
| 11.05 (pre-#08.5) | UX-чистка + защита твёрдой земли. §4.1 `billing_frequency` — Show? = false (удаление столбца из Sheets отложено в pre-#08.6). §4.2 `students_visible_Form` очищен от VC и Related. §4.3 inline платежи — закрыт по факту (table view уже информативен). §4.4 Edit charges из закрытых периодов запрещён: `[period] = INDEX(settings[current_period], 1)` на системном Edit (Show_If доступен, не read-only — отличие от системного Add из pre-#08.3 SKILL Q.17). §4.5 фиксация источника `charge_type`: Initial value = «Ручное» (уже было), поле убрано из `charges_Form`, бот через `student_tariffs__create_next_charge` явно пишет «Авто». Зафиксирован паттерн проекта `INDEX(settings[<col>], 1)` (не `ANY(...)`). Зафиксирован двухуровневый канал правки: AppSheet UI блокирует закрытое, прямое редактирование Sheets — аварийный канал владельца (§7.1). Версия 1.000518 → 1.000523. |
| 11.05 (pre-#08.6) | Закрытие рудимента + полное распространение защиты «закрытый период = read-only». §4.1 `billing_frequency` удалён из Sheets, Regenerate Schema без ошибок. §4.2 защита от создания charges в закрытом периоде: на `charges[period]` Initial value `INDEX(settings[current_period], 1)` и Valid_if `SELECT(ref_periods[period], AND(NUMBER([period]) >= NUMBER(INDEX(settings[current_period], 1)), NUMBER([period]) <= NUMBER(INDEX(settings[current_period], 1)) + 3))` — окно current..current+3; `ref_periods` → Read-Only (исчезла «+ New»). Плановое расширение по решению стратега в ходе сессии: Edit + Delete защищены от закрытого периода на всех трёх таблицах — charges (`[period] = current`), payments (`TEXT([payment_date], "YYYYMM") = current` — у платежей нет колонки period, период выводится из payment_date), expenses (`[period] = current`). Slice'ы `charges_filtered`, `payments_filtered`, `expenses_filtered` переведены из Read-Only в Updates+Deletes — обнаружен **паттерн «slice Read-Only блокирует системные actions в slice-Detail»** (см. SKILL). Попутно исправлен баг `expenses[period]` Valid_if (была `- 3` на нижней границе, разрешала ретро). Версия 1.000523 → 1.000535 (платформа обновилась в ходе сессии). |

---

## §10. История версий документа

- **0.1** (07.05.2026) — первый черновик с пробелами.
- **0.2** (07.05.2026) — расширен на основе бэкапов v3–v7. Добавлены метафоры, ADR, эволюция, дашборд, бизнес-процессы.
- **0.3** (08.05.2026) — синхронизация с бэкапами v8–v12.1. Закрыт ТД-1 (Р-16), описано закрытие периода через AppSheet Bot, добавлена ТД-5, актуализированы открытые вопросы и хронология. Технические детали вынесены в `ARCHITECTURE.md`, регламенты — в `REGULATIONS.md`.
- **0.4** (09.05.2026) — синхронизация с отчётами pre-#08 и pre-#08.1. §4.2 Ученик расширен (`birth_date`, выбытие = очистка class_group + удаление student_tariffs, фильтр `students_visible`). §5.4 Выбытие переписано под финальный поток (Confirmation → форма даты → транзакция). §8.4 — три новых технических хвоста (`_ComputedName`, VC vs Column order, предупреждение в form). §9 — хронология продлена до 09.05.
- **0.4.1** (09.05.2026) — закрыт хвост `_ComputedName` (Show=false при сохранённом LABEL). Уточнён хвост «VC vs Column order»: VC-скаляры рендерятся между физическими и Related-блоками, не в самом конце.
- **0.4.2** (09.05.2026) — переписан §5.6 «Летний цикл» с правильной логикой дат (массовая замена привязок ДО закрытия мая, а не после). Зафиксированы решения по тарифам: T010 = 5 000 ₽ временно, T009 удалить, июнь-июль-август все по T010. Сняты три `[?? уточнить]` из §8.3.
- **0.4.3** (10.05.2026) — зафиксировано решение по `tariff_type`: все тарифы по определению регулярные, колонка-рудимент удаляется в pre-#08.4. §4.3 переписан, ТД-5 расширен (`tariff_type` + view-фильтры), §8.4 синхронизирован.
- **0.4.4** (10.05.2026) — добавлены в §8.4: ограничение AppSheet по раздельным заголовкам Add/Edit (компромисс — VC `Section_Header`, см. SKILL Q.16/Q.17); открытый хвост по запрету Edit для «Авто»-charges; пять смежных хвостов от Sonnet'а из сессии pre-#08.3 (`billing_frequency`, `tariff_type` в Detail, чистка `students_visible_Form`, `birth_date` Initial value, header для inline платежей).
- **0.4.5** (10.05.2026) — синхронизация с отчётом pre-#08.3. §7.2: версия AppSheet 1.000489 → 1.000507. §8.4: закрыты два хвоста (`tariff_type` в `tariffs_Detail`, `birth_date` Initial value — оба сделал владелец вручную в сессии). Добавлен новый хвост: Detail view charges/payments открывается через `*_filtered`, не системный `*_Detail` — Header column в `*_Detail` визуально не проявится. §9: продлена хронология строкой по pre-#08.3.
- **0.4.6** (10.05.2026, вечер) — после первой попытки исполнения pre-#08.4 встроенный фильтр AppSheet перенесён из задачи в исследование. На view «начисления» список фильтруемых колонок не совпал с view «ученики», правило не понято. `*_filtered` оставлены работать. ТД-5 и §8.4 переписаны под новое состояние. Pre-#08.4 ужат до одной задачи (`tariff_type`).
- **0.4.7** (11.05.2026) — синхронизация с отчётом pre-#08.4. §4.3, §7.2, §8.1 ТД-5, §8.4 — отметка «закрыт» по `tariff_type` (удалена), фиксация дополнительной правки `settings[current_period]`. §8.4 пункт по View-фильтрам — добавлено наблюдение Sonnet'а о Filter panel на временном view `начисления` (Number/Enum не появились — гипотеза «только скаляры» не подтверждена). §9 — продлена хронология строкой по pre-#08.4.
- **0.4.8** (11.05.2026) — синхронизация с отчётом pre-#08.5. §7.1: добавлен абзац о двухуровневом канале правки данных (AppSheet UI vs прямое Sheets). §7.2: версия 1.000507+ → 1.000523. §8.4: закрыты хвосты «запрет Edit charges», `students_visible_Form`, Header column inline платежей; статус `billing_frequency` → «частично закрыт» (Show? = false, Sheets-удаление в pre-#08.6). Добавлены два новых хвоста на pre-#08.6: защита от создания charges в закрытом периоде (Valid_if на `charges[period]` с диапазоном current+3) и удаление столбца `billing_frequency` из Sheets. §9 — продлена хронология строкой по pre-#08.5, скорректирована версия pre-#08.4 (фактическая на старте pre-#08.5 — 1.000518).
- **0.4.9** (11.05.2026) — синхронизация с отчётом pre-#08.6. §7.2: версия 1.000523 → 1.000535. §8.4: закрыты хвосты `billing_frequency` (удалён из Sheets) и «защита от создания charges в закрытом периоде» (Initial value + Valid_if на `charges[period]`, `ref_periods` → Read-Only). Добавлены три новых хвоста: Delete не виден на мобильном через slice-Detail для charges/expenses (десктоп корректен, не блокер); ревизия остальных slice'ов на Read-Only; ревизия всех Valid_if на колонках с Ref на `ref_periods` на наличие `- 3`. §9 — продлена хронология строкой по pre-#08.6 (полное распространение защиты «закрытый период = read-only» на Edit и Delete для charges/payments/expenses, паттерн slice Read-Only).

`[?? после ответов на оставшиеся пробелы → v1.0]`

---

*Этот документ описывает систему «снаружи». Технические детали — в ARCHITECTURE.md. Регламенты работы — в REGULATIONS.md. Словарь — в GLOSSARY.md.*
